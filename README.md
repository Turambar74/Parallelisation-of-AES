# Parallelisation-of-AES
(Group of three)

Performs the encryption of an input message provided by the user using AES in counter mode in parallel. Implemented separately using MPI and CUDA frameworks.

